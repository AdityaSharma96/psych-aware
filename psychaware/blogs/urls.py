from django.contrib import admin
from django.urls import path
from . import views

urlpatterns = [
    # path('write', views.blog_write, name="blog_write"),
    # path('view/',views.view_blog,name="view_blog"),
    # path('read/<str:blog_identifier>', views.blog_read, name="blog_read"),
]
